/**
 * Activities Data
 * 
 * NOTE: This file previously contained mock activity data.
 * Mock data has been replaced with real Supabase queries.
 * 
 * All activities now come from the database via /src/services/api.ts
 * Use fetchActivities() to get the current list of activities.
 * 
 * See /src/types/index.ts for Activity type definition.
 */

import type { Activity } from "../../types";

// Mock data has been removed - data now comes from Supabase
// See /src/services/api.ts for data fetching:
// - fetchActivities()

export type { Activity };
